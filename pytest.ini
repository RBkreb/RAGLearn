[pytest]
# Unit/integration/e2e tests live here. The `tests/evals/` directory is a
# DeepEval evaluation harness (requires a running local LM Studio), not a
# pytest suite, so keep it out of collection.
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
